{
  "name": "omschatgptapp",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "sh -c \"npm run dev:backend & npm run dev:mcp & npm run dev:frontend & wait\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:mcp": "cd mcp-server && npm run dev",
    "build:widget": "cd frontend && npm ci && npm run build",
    "build:frontend": "cd frontend && npm ci && npm run build",
    "build:mcp": "cd mcp-server && npm ci && npm run build",
    "copy:widget": "node scripts/copy-widget.js",
    "verify:widget": "node scripts/verify-widget-build.js",
    "build": "npm run build:widget && npm run verify:widget && npm run build:mcp && npm run copy:widget",
    "start:mcp": "cd mcp-server && npm start"
  }
}
